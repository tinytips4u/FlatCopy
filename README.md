# FlatCopy
Copy files from a directory and all its subdirectories into another directory. No folder structure will be maintained. Files with the same name will be suffixed with a number. </br></br>
Syntax:</br>
`flatcopy.exe -src "FULL_PATH_TO_SOURCE_FOLDER" -dst "FULL_PATH_TO_DESTINATION_FOLDER"`
